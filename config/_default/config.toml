# -- Site Configuration --
# Refer to the theme docs for more details about each of these parameters.
# https://jpanther.github.io/congo/docs/getting-started/

baseURL = "https://pitemarx.github.io/"
defaultContentLanguage = "en"

enableRobotsTXT = true
paginate = 40
summaryLength = 50

[outputs]
  home = ["HTML", "RSS", "JSON"]
